\section{Mechanism-Gated Self-Evolution}
\label{sec:method}

\subsection{Immutable candidate lifecycle}

Each round executes four stages: observe, diagnose, mutate, and verify.
Exploration trajectories are immutable and role-filtered.  A diagnosis cites
specific trajectory spans, localizes a failure to an editable surface, and
predicts both an intended effect and plausible regressions.  A typed mutation
is applied to exactly one parent snapshot, structurally validated, and assigned
a content hash before execution.  The proposer cannot rewrite an earlier
artifact or its own evaluation policy.

For the same-model condition, one provider-resolved model identity and revision
fills solver, diagnosis, proposal, materialization planning, candidate ranking,
and nomination roles; tokenizer, runtime, and role-specific inference settings
are frozen before search and matched across experimental policies.  All role
calls are recorded separately.  Final promotion is not a model role: the
independent authority applies the frozen statistical rule.  A claim of
\emph{same-model self-evolution} requires that the exact model configuration
close every model-mediated role without manual candidate choice or a stronger
teacher.  An external stronger proposer is a valid harness-optimization
baseline, but answers a different question and is labeled accordingly.

\subsection{Matched interventions}

For each gate nomination, the verifier constructs a matched quartet:
\begin{enumerate}
  \item \textbf{child}: the nominated harness with the mutation enabled;
  \item \textbf{parent}: the exact champion from which the child descends;
  \item \textbf{revert}: the child runtime with the mutation exactly disabled,
  retaining other candidate-path overhead; and
  \item \textbf{placebo}: a format- and budget-matched neutral mutation that
  should not encode the proposed mechanism.
\end{enumerate}

\begin{figure}[ht]
  \centering
  \input{figures/mechanism_boundary.tex}
  \caption{Target mechanism and authority boundary.  The mutable optimizer can edit
  only declared harness surfaces.  Promotion belongs to a separate authority
  that executes matched interventions and fails closed along the mechanism
  chain.  Sealed tasks are released once, only after all search cells close.
  Solid arrows carry typed artifacts; the dashed arrow carries a redacted
  decision, never sealed item feedback.  Shape and labels preserve the meaning
  in grayscale.  In the target deployment, after closure, the sealed authority audits the frozen commit
  registry and champions once.}
  \label{fig:mechanism-boundary}
\end{figure}

Task identity, rollout seed, model settings, runtime image, and resource ceiling
are paired.  Arm order is randomized by a verifier-owned schedule.  Revert
tests whether the declared edit matters; placebo tests whether merely adding a
similarly shaped artifact or execution path explains the observation.

\subsection{Fail-closed promotion gate}

The gate evaluates source-linked claims in causal order.  Later success cannot
repair an earlier missing link:
\[
\promote(c)=
\mathbb{1}\{A_c\land D_c\land B_c\land U_c\land P_c\land C_c\land L_c\},
\]
where \(A,D,B\) denote activation, adherence, and intended behavior; \(U\)
denotes utility benefit; \(P\) protected-slice non-inferiority; \(C\) the cost
constraint; and \(L\) protocol, provenance, and leakage validity.  Every term is
three-valued---pass, fail, or inconclusive---and only all-pass promotes.  Safety
and cost remain separate constraints rather than disappearing inside a weighted
score.

Activation and protocol validity are exact, source-bound predicates.  For every
stochastic adherence, behavior, utility, protection, or cost term, the task
manifest must freeze the paired child--parent and required child--revert/placebo
estimands, nulls, operationally justified margins, missingness rule, and test.
A nomination spends predictable alpha for every superiority or non-inferiority
null it must reject; the sum over nomination, policy, and gate dimension is at
most the campaign budget.  A model-judged link is usable only after disjoint
calibration, with prespecified judge-error bounds propagated through the gate;
otherwise that link is inconclusive.

The target gate and later sealed audit must bind the same utility rubric, benefit
margin, protected slices, non-inferiority margins, cost rule, and missingness
mapping.  Gate blocks authorize commits during search; sealed outcomes never do.
The atomic audit instead measures whether those already-frozen decisions
generalize.  This separation prevents a favorable sealed result from becoming
another optimization round.

The verifier spends a frozen campaign-level error budget across all possible
nominations.  A conservative fixed-horizon allocation is the default; a
time-uniform procedure may replace it only when preregistered before outcomes
are visible \citep{howard2021confidence}.  The optimizer sees only evidence
authorized for its condition and never sealed outcomes.  Rejected and
inconclusive candidates remain in the ledger, so failures cannot vanish through
complete-case reporting.

\subsection{Independent selection and sealing}

The target promotion authority must be outside every candidate's editable
surface and own task manifests, paired schedules, graders, evidence producers,
usage accounting, and the champion pointer.  After every condition and
independent search run reaches a terminal selection state, a separate sealed
service must accept the authority-signed registry of every committed edit and
final champion hash.  One atomic authorization must evaluate the full registry and grid under the same
benefit and non-inferiority definitions, then releases signed aggregates; it
never returns item outputs or trajectories to the optimizer.  Opening any
sealed item early invalidates that study lineage.  A failed sealed result cannot
trigger more tuning or sample-size continuation in that lineage; a changed
method requires a new preregistration and an independently committed sealed set.
